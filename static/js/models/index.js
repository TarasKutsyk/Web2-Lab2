const {Cargo, DeliveredCargo} = require('./Cargo');
const Planet = require('./Planet');
const {SpaceStation, OrbitStation} = require('./SpaceStation');
const Collection = require('./Collection');

module.exports = {
  Cargo, Planet, SpaceStation, Collection
};
